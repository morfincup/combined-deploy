server {
  listen 80;
  server_name _;

  # Landing
  root /usr/share/nginx/html/root;
  index index.html;

  location = / {
    try_files /index.html =404;
  }

  # --- SHEF (static) ---
  location /shef/ {
    alias /usr/share/nginx/html/shef/;
    try_files $uri $uri/ /shef/index.html;
  }

  # --- PREMIUM (static) ---
  location /premium/ {
    alias /usr/share/nginx/html/premium/;
    try_files $uri $uri/ /premium/index.html;
  }

  # --- APIs ---

